feat: persist videos with supabase

## Schema
- Created `videos` table with columns: id, title, description, thumbnail_url, video_url, platform (twitch/tiktok), category, duration, views, published_at, featured, created_at, updated_at
- Added indexes on published_at, featured, platform
- Added trigger for updated_at

## RLS Policies
- Public SELECT for anon + authenticated (all videos visible)
- INSERT/UPDATE/DELETE restricted to admin (false condition for now - no public writes)
- RLS enabled on table

## Data Layer
- `src/lib/videos.ts` updated with Supabase integration + mock fallback
- `Video`, `VideoFilters`, `VideoSort` types added to `src/lib/supabase.ts`
- `videosRepository` interface preserved for UI consumption
- Mock data (`MOCK_VIDEOS`) preserved as fallback when Supabase unavailable
- Repository auto-detects Supabase availability

## Seed
- Created `20260910000001_seed_videos.sql` with 12 videos from existing mock data
- UPSERT logic preserves existing records
- Titles, URLs, platforms, categories, durations, views preserved

## Migration
- `20260910000000_create_videos_table.sql` - table + RLS + indexes + trigger
- `20260910000001_seed_videos.sql` - 12 video records (upsert)

## Pages
- `Videos.tsx` uses repository (unchanged API)
- `Home.tsx` uses `videosRepository.findFeatured(3)` (shared data layer)
- No breaking changes to UI components

## Validation
- typecheck: PASS
- lint: PASS (4 pre-existing react-refresh warnings)
- build: PASS (408 kB JS / 116 kB gzipped)
- Playwright: Home, Videos, Community, Profile, Wishlist, Auth, Terms, Privacy — desktop/tablet/mobile ✅
- Console: 0 errors (404s are expected for mock thumbnails)
- Page titles: "nicotinacat" (no Hub)

## Residual References (Legitimate)
- Terms.tsx: "Arcade" in legal terms (descriptive)
- videos.ts: title "Reacting to Your Clips" (historical content)

## Pending for Future Missions
- Tabela Supabase `videos` with RLS in production
- Integração real TikTok/Twitch (OAuth, sync, thumbnails)
- Player embed nativo
- Upload/gerenciamento de vídeos
- Admin/moderation